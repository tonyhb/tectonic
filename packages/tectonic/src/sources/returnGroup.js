'use strict';

/**
 * ReturnGroup is used within a SourceDefinition to specify what an API returns.
 *
 * It can hold one or many Returns instances; having one instance specifies that
 * the API returns one model whereas many specifies that the API returns many
 * model's data.
 *
 * TODO: Fully implement and use
 */
/*
class ReturnGroup {

  constructor(data) {
    // The user specified that the source definition returns more than one
    // model.
    if (typeof data === 'object') {
    }
  }

}
*/
